# ERP Database Schema

This document outlines the logical table/collection structures and column definitions for the Inventory ERP system.

## Implemented in Phase 1

### Users
| Column | Type | Notes |
|---|---|---|
| id | string | `USR-000001`, generated by the app |
| name | string | |
| email | string | unique, lowercased |
| passwordHash | string | bcrypt hash, never plain text |
| role | string | Admin / Manager / Sales User / Accountant / HR |
| status | string | Active / Inactive |
| createdAt | ISO datetime | set by the app |
| updatedAt | ISO datetime | set by the app |

## Implemented in Phase 2

### Categories
| Column | Type | Notes |
|---|---|---|
| id | string | `CAT-000001` |
| name | string | unique |
| status | string | Active / Inactive |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

### Brands
| Column | Type | Notes |
|---|---|---|
| id | string | `BRD-000001` |
| name | string | unique |
| status | string | Active / Inactive |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

### Units
| Column | Type | Notes |
|---|---|---|
| id | string | `UNT-000001` |
| name | string | e.g. "Piece", "Box" |
| status | string | Active / Inactive |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

### Products
| Column | Type | Notes |
|---|---|---|
| id | string | `PROD-000001` |
| sku | string | unique, optional |
| name | string | |
| categoryId | string | FK → Categories.id |
| brandId | string | FK → Brands.id |
| unitId | string | FK → Units.id |
| description | string | |
| purchasePrice | number | |
| pricingMethod | string | "Percentage Markup" / "Fixed Selling Price" |
| markupPercentage | number | blank if Fixed Selling Price |
| sellingPrice | number | auto-calculated or manually fixed |
| minimumStock | number | for low-stock alerts |
| openingStock | number | write-once at creation; NOT the live stock count |
| batchNumber | string | |
| expiryDate | date | |
| status | string | Active / Inactive |
| priceEffectiveDate | ISO datetime | last time price changed |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

**Important:** there is deliberately no "current stock" column. Current
stock is always computed on the fly by summing `Stock_Transactions` for
that product (see `StockTransactionRepository.computeCurrentStock`).

### Stock_Transactions
| Column | Type | Notes |
|---|---|---|
| id | string | `STK-000001` |
| productId | string | FK → Products.id |
| type | string | Opening Stock / Purchase / Sale / Sales Return / Purchase Return / Stock Adjustment In / Stock Adjustment Out / Damaged / Expired |
| quantity | number | always stored positive; direction comes from `type` |
| referenceType | string | e.g. "Purchase", "Sale", "Manual" |
| referenceId | string | ID of the purchase/sale/etc that caused this, if any |
| note | string | |
| createdBy | string | user email |
| transactionDate | ISO datetime | |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

### Product_Price_History
*(Not in the original 27-sheet list — added because spec section 47 requires
an audit trail of price changes that never touches past invoices.)*

| Column | Type | Notes |
|---|---|---|
| id | string | `PPH-000001` |
| productId | string | FK → Products.id |
| previousPurchasePrice | number | |
| newPurchasePrice | number | |
| previousMarkup | number | |
| newMarkup | number | |
| previousSellingPrice | number | |
| newSellingPrice | number | |
| effectiveDate | ISO datetime | |
| changedBy | string | user email |
| reason | string | |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

## Implemented in Phase 3

### Customers
| Column | Type | Notes |
|---|---|---|
| id | string | `CUS-000001` |
| name | string | |
| phone | string | |
| email | string | optional |
| address | string | |
| openingBalance | number | informational only — set once at creation, seeds a ledger entry; never the live balance |
| status | string | Active / Inactive |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

### Customer_Transactions
| Column | Type | Notes |
|---|---|---|
| id | string | `CTX-000001` |
| customerId | string | FK → Customers.id |
| type | string | Opening Balance / Invoice / Payment Received / Sales Return / Adjustment Increase / Adjustment Decrease |
| amount | number | always stored positive; direction comes from `type` |
| referenceType | string | e.g. "Sale", "Manual" |
| referenceId | string | ID of the invoice/payment that caused this, if any |
| note | string | |
| createdBy | string | user email |
| transactionDate | ISO datetime | |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

**Balance due** is always computed by summing this ledger (Opening Balance /
Invoice / Adjustment Increase add to what they owe; Payment Received /
Sales Return / Adjustment Decrease subtract). There is no stored balance
field on Customers — same principle as Product stock.

### Suppliers
| Column | Type | Notes |
|---|---|---|
| id | string | `SUP-000001` |
| name | string | |
| phone | string | |
| email | string | optional |
| address | string | |
| openingBalance | number | informational only — seeds a ledger entry at creation |
| status | string | Active / Inactive |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

### Supplier_Transactions
| Column | Type | Notes |
|---|---|---|
| id | string | `STX-000001` |
| supplierId | string | FK → Suppliers.id |
| type | string | Opening Balance / Purchase / Payment Made / Purchase Return / Adjustment Increase / Adjustment Decrease |
| amount | number | always stored positive; direction comes from `type` |
| referenceType | string | e.g. "Purchase", "Manual" |
| referenceId | string | |
| note | string | |
| createdBy | string | user email |
| transactionDate | ISO datetime | |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

Same computed-balance principle: Opening Balance / Purchase / Adjustment
Increase add to what you owe the supplier; Payment Made / Purchase Return /
Adjustment Decrease subtract.

## Implemented in Phase 4

### Sales
| Column | Type | Notes |
|---|---|---|
| id | string | `INV-000001` |
| customerId | string | FK → Customers.id |
| saleDate | ISO datetime | date of the sale |
| subtotal | number | sum of line totals before discount |
| discount | number | flat discount amount (≥ 0) |
| total | number | subtotal − discount |
| amountPaid | number | cash/bank collected at time of sale |
| paymentMethod | string | Cash / Bank / Credit |
| status | string | Completed / Cancelled |
| note | string | |
| createdBy | string | user email |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

Creating a sale also writes:
- one `Sale_Items` row per product line (price/cost snapshots)
- one `Stock_Transactions` row per line (`type: Sale`)
- one `Customer_Transactions` row (`type: Invoice`, amount = total)
- optional `Customer_Transactions` row (`type: Payment Received`, amount = amountPaid)

### Sale_Items
| Column | Type | Notes |
|---|---|---|
| id | string | `SIT-000001` |
| saleId | string | FK → Sales.id |
| productId | string | FK → Products.id |
| productName | string | snapshot at sale time |
| quantity | number | |
| unitPrice | number | selling price snapshot |
| unitCost | number | purchase price snapshot (for profit reporting) |
| lineTotal | number | quantity × unitPrice |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

### Purchases
| Column | Type | Notes |
|---|---|---|
| id | string | `PUR-000001` |
| supplierId | string | FK → Suppliers.id |
| purchaseDate | ISO datetime | |
| subtotal | number | |
| discount | number | |
| total | number | |
| amountPaid | number | paid to supplier at time of purchase |
| paymentMethod | string | Cash / Bank / Credit |
| status | string | Completed / Cancelled |
| note | string | |
| createdBy | string | user email |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

Creating a purchase also writes:
- one `Purchase_Items` row per product line
- one `Stock_Transactions` row per line (`type: Purchase`)
- one `Supplier_Transactions` row (`type: Purchase`, amount = total)
- optional `Supplier_Transactions` row (`type: Payment Made`, amount = amountPaid)

### Purchase_Items
| Column | Type | Notes |
|---|---|---|
| id | string | `PIT-000001` |
| purchaseId | string | FK → Purchases.id |
| productId | string | FK → Products.id |
| productName | string | snapshot |
| quantity | number | |
| unitCost | number | purchase price snapshot |
| lineTotal | number | quantity × unitCost |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

## Implemented in Phase 5

### Challans
| Column | Type | Notes |
|---|---|---|
| id | string | `CHL-000001` |
| customerId | string | FK → Customers.id |
| saleId | string | optional FK → Sales.id |
| challanDate | ISO datetime | |
| status | string | Dispatched / Cancelled |
| note | string | |
| deductStock | string | Yes / No — whether stock was deducted on creation |
| createdBy | string | user email |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

Standalone challans deduct stock (`type: Sale`, `referenceType: Challan`).
Linked-to-sale challans are delivery notes only (stock already moved at sale time).

### Challan_Items
| Column | Type | Notes |
|---|---|---|
| id | string | `CIT-000001` |
| challanId | string | FK → Challans.id |
| productId | string | FK → Products.id |
| productName | string | snapshot |
| quantity | number | |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

### Payments
| Column | Type | Notes |
|---|---|---|
| id | string | `PAY-000001` |
| partyType | string | Customer / Supplier |
| partyId | string | FK → Customers.id or Suppliers.id |
| direction | string | Received (customer) / Paid (supplier) |
| amount | number | |
| paymentMethod | string | Cash / Bank |
| referenceType | string | e.g. Manual, Payment |
| referenceId | string | |
| note | string | |
| paymentDate | ISO datetime | |
| createdBy | string | user email |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

Also writes a matching `Customer_Transactions` or `Supplier_Transactions` row.

### Sale_Returns
| Column | Type | Notes |
|---|---|---|
| id | string | `SRN-000001` |
| saleId | string | FK → Sales.id |
| customerId | string | FK → Customers.id |
| returnDate | ISO datetime | |
| subtotal | number | |
| total | number | proportional discount applied |
| note | string | |
| status | string | Completed |
| createdBy | string | |
| createdAt | ISO datetime | |
| updatedAt | ISO datetime | |

### Sale_Return_Items
| id, saleReturnId, productId, productName, quantity, unitPrice, lineTotal, createdAt, updatedAt |

### Purchase_Returns / Purchase_Return_Items
Same pattern as sale returns with `PRN-*` / `PRI-*` prefixes, linked to Purchases.

**Cancellations** set Sales/Purchases `status` to Cancelled and write reversing stock + ledger entries.
Partial returns use the return tables above; full cancel is blocked if any returns already exist.

## Planned for later phases (create tabs now if you want, columns TBD per phase)

19. Expenses
20. Expense_Categories
21. Employees
22. Attendance
23. Salary
24. Salary_Advances
25. Cash_Bank_Transactions
26. Settings
27. Audit_Logs

Each of these gets its own schema and repository class,
following the pattern used for `Users`
(`backend/repositories/mongo/UserRepository.js`).

## ID conventions

| Entity | Prefix | Example |
|---|---|---|
| User | USR | USR-000001 |
| Category | CAT | CAT-000001 |
| Brand | BRD | BRD-000001 |
| Unit | UNT | UNT-000001 |
| Product | PROD | PROD-000001 |
| Stock Transaction | STK | STK-000001 |
| Product Price History | PPH | PPH-000001 |
| Customer | CUS | CUS-000001 |
| Customer Transaction | CTX | CTX-000001 |
| Supplier | SUP | SUP-000001 |
| Supplier Transaction | STX | STX-000001 |
| Invoice (Sale) | INV | INV-000001 |
| Sale Item | SIT | SIT-000001 |
| Purchase | PUR | PUR-000001 |
| Purchase Item | PIT | PIT-000001 |
| Challan | CHL | CHL-000001 |
| Challan Item | CIT | CIT-000001 |
| Payment | PAY | PAY-000001 |
| Sale Return | SRN | SRN-000001 |
| Sale Return Item | SRI | SRI-000001 |
| Purchase Return | PRN | PRN-000001 |
| Purchase Return Item | PRI | PRI-000001 |
| Employee | EMP | EMP-000001 |
| Expense | EXP | EXP-000001 |
| Patient | PAT | PAT-000001 |
| Diagnostic Test | DTEST | DTEST-000001 |
| Diagnostic Order | DORD | DORD-000001 |
| Diagnostic Result | DRES | DRES-000001 |
| Doctor Commission | DCOM | DCOM-000001 |
| Clinical Audit Log | AUD | AUD-000001 |

IDs are never derived from row position — see `backend/utils/idGenerator.js`.

## Clinical Diagnostic Information System (HIS / LIS / RIS)

### Patients (`patients`)
Multi-tenant UHID patient registration entity.
- `tenantId` (string, indexed)
- `uhid` (string, unique per tenant format: `UHID-TENANT-YYYYMMDD-XXXX`)
- `fullName` (string)
- `gender` (Male / Female / Other)
- `age` ({ value: number, unit: 'Years'|'Months'|'Days' })
- `phone` (string, indexed)
- `bloodGroup` (A+, A-, B+, B-, AB+, AB-, O+, O-, Unknown)
- `emergencyContact` ({ name, relationship, phone })
- `referredDoctor` ({ name, hospital, contact })

### Diagnostic Tests (`diagnostic_tests`)
Pathology reference range parameters & Radiology modality master catalog.
- `tenantId` (string, indexed)
- `code` (string, e.g. `LAB-CBC`, `RAD-MRI-BRAIN`)
- `name` (string)
- `department` (Pathology / Radiology / Cardiology / Other)
- `category` (Hematology, Biochemistry, MRI, CT Scan, X-Ray, USG)
- `price` (number)
- `parameters` array (parameterName, unit, referenceRanges: [{ gender, minAge, maxAge, rangeLow, rangeHigh }])
- `radiologyDetails` ({ modality, bodyPart, instructions })

### Diagnostic Orders (`diagnostic_orders`)
Patient billing invoice and specimen tube worklist.
- `tenantId` (string, indexed)
- `invoiceNumber` (string, format: `INV-YYYYMMDD-XXXX`)
- `orderBarcode` (string, Code128 string: `INV-<TENANT>-<TIMESTAMP>`)
- `uhid` (string, indexed)
- `patientSnapshot` ({ fullName, age, gender, phone })
- `tests` array ({ testId, testCode, testName, department, price, specimenBarcode, sampleStatus })
- `financials` ({ totalAmount, discountAmount, taxAmount, netAmount, paidAmount, dueAmount, paymentStatus })
- `isEmergency` (boolean, Trauma Break-Glass flag)
- `status` (pending / in_progress / completed / cancelled)

### Diagnostic Results (`diagnostic_results`)
Multi-stage LIS Pathology parameters and RIS Radiology narrative reports.
- `tenantId` (string, indexed)
- `orderId` (string, indexed)
- `invoiceNumber` (string)
- `uhid` (string, indexed)
- `specimenBarcode` (string, indexed)
- `department` (Pathology / Radiology)
- `status` (pending -> sample_collected -> result_ready -> authorized)
- `pathologyResults` array ({ parameterName, resultValue, unit, referenceRange, isCritical, remarks })
- `radiologyReport` ({ clinicalHistory, technique, findings, impression, dcmStudyInstanceUID, attachmentUrls })
- `authorizedBy` (string)
- `digitalSignature` ({ signatureHash, signatureUrl, signedBy })

### Doctor Commissions (`doctor_commissions`)
Referring doctor referral commission ledger and payout tracking.
- `tenantId` (string, indexed)
- `doctorName` (string, indexed)
- `orderId` (string)
- `invoiceNumber` (string)
- `totalOrderAmount` (number)
- `commissionRate` (number, default 10%)
- `commissionAmount` (number)
- `payoutStatus` (Unpaid / Paid)
- `paidAt` (datetime)

### Clinical Audit Logs (`clinical_audit_logs`)
HIPAA/PHI compliance access control and trauma break-glass audit trail.
- `tenantId` (string, indexed)
- `userId` (string)
- `userRole` (string)
- `action` (VIEW / EDIT / DELETE / AUTHORIZE / PRINT / BREAK_GLASS)
- `entity` (Patient / DiagnosticOrder / DiagnosticResult)
- `entityId` (string)
- `ipAddress` (string)
- `details` (string)

